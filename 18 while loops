#include<iostream>
#include<string>

int main() 
{
	using namespace std;

	//string name;
	/*while (name.empty()) {
		cout << "Enter your name: ";
		getline(cin, name);
	}
	
	cout << "hey " << name;*/

	while (true) { //its like a while loop that never ends similar to (1=1)
		cout << "oh no, its a trap!";
	}

	return 0;
}
