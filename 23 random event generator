#include<iostream>
#include<string>

int main() 
{
	using namespace std;

	// pseudo-random = NOT truly random, but generated by a deterministic process

	srand(time(NULL)); // seed the random number generator with the current time

	//int num = (rand() % 6) + 1; // generate a random number between 1 and 6 for a dice roll
	//cout << num;

	//int num = (rand() % 20) + 1; // generate a random number between 1 and 20 for a polyhedral dice roll
	//cout << num;

	int num1 = (rand() % 6) + 1; // generate a random number between 1 and 6 for 3 dice roll
	int num2 = (rand() % 6) + 1;
	int num3 = (rand() % 6) + 1;

	cout << num1 << '\n';
	cout << num2 << '\n';
	cout << num3 << '\n';

	return 0;
}
