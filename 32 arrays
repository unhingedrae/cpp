#include<iostream>
#include<string>
#include<ctime> 
#include<iomanip>

int main() {

    using namespace std;

	//arrays = a data structure that can hold more than one value at a time
	//values are accessed by an index number
	//"kind of like a list of values"

	//string car[] = { "BMW", "Mercedes", "Audi" }; //array of strings "[] = {}"

	//cout << car; //this wont work, it will show some random numbers and text such as "0000007A0F6FFA40",
	//this is a memory address of where our array is located
	//if we need one of the elements within the array, we have to add index number, after the array name

	//cout << car[0] << '\n'; //first element of the array starts from 0
	//cout << car[1] << '\n';
	//cout << car[2] << '\n';

	//you can reassign values too 

	//string car[] = { "BMW", "Mercedes", "Audi" };
	//car[0] = "Cadillac";

	//cout << car[0] << '\n';
	//cout << car[1] << '\n';
	//cout << car[2] << '\n';

	//one thing with arrays is that they only contain values of the same data type, for instance you cant add string with numbers, etc
	//string car[] = { "BMW", "Mercedes", "Audi", "1" };

	//you can declare an array if you dont know what you gonna add.

	/*string car[];

	car[0] = "Cadillac";
	car[1] = "Mercedes";
	car[2] = "Audi";

	cout << car[0] << '\n';
	cout << car[1] << '\n';
	cout << car[2] << '\n';*/

	//we will have an error if do the above example, cause it misses array size.
	//we can declare an array and then assign values later, we need to set a size to this array, its a static data struct
	//if i am going to place only 3 cars, within this array, i will write 3 inside the "string car[3]

	string car[3];

	car[0] = "Cadillac";
	car[1] = "Mercedes";
	car[2] = "Audi";

	cout << car[0] << '\n';
	cout << car[1] << '\n';
	cout << car[2] << '\n';

	return 0;
}
example 2:

#include<iostream>
#include<string>
#include<ctime> 
#include<iomanip>

int main() {

    using namespace std;

	double price[] = {2.5, 5.6, 8.7, 9.9, 10.80, 100.77};

	cout << price[0] << '\n';
	cout << price[1] << '\n';
	cout << price[2] << '\n';
	cout << price[3] << '\n';
	cout << price[4] << '\n';
	cout << price[5] << '\n';

	return 0;
}
