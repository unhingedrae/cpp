for ascending order: 
#include<iostream>
#include<string>
#include<ctime> 
#include<iomanip>

void sort(int array[], int size);

int main() {

	using namespace std;

	int array[] = { 10, 5, 7, 3, 9, 4, 2, 6, 8, 1 };
	int size = sizeof(array) / sizeof(int);

	sort(array, size);

	for (int element : array) {
		cout << element << " ";
	}

	return 0;
}
void sort(int array[], int size) {
	int temp;
	for (int i = 0; i < size - 1; i++) {
		for (int j = 0; j < size - i - 1; j++) {
			if (array[j] > array[j + 1]) {
				temp = array[j];
				array[j] = array[j + 1];
				array[j + 1] = temp;
			}
		}
	}
}

example 2: for descending order

#include<iostream>
#include<string>
#include<ctime> 
#include<iomanip>

void sort(int array[], int size);

int main() {

	using namespace std;

	int array[] = { 10, 5, 7, 3, 9, 4, 2, 6, 8, 1 };
	int size = sizeof(array) / sizeof(int);

	sort(array, size);

	for (int element : array) {
		cout << element << " ";
	}

	return 0;
}
void sort(int array[], int size) {
	int temp;
	for (int i = 0; i < size - 1; i++) {
		for (int j = 0; j < size - i - 1; j++) {
			if (array[j] < array[j + 1]) { //for descending order "<"
				temp = array[j];
				array[j] = array[j + 1];
				array[j + 1] = temp;
			}
		}
	}
}
